name: Build APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle 8.6
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: '8.6'

      - name: Install Android SDK components
        run: |
          yes | "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" --licenses > /dev/null || true
          "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" "platforms;android-34" "build-tools;34.0.0" > /dev/null

      - name: Run unit tests
        run: gradle -p "$GITHUB_WORKSPACE" testDebugUnitTest --no-daemon --stacktrace

      - name: Restore signing keystore
        env:
          ZIP_PASSWORD: ${{ secrets.SIGNING_ZIP_PASSWORD }}
        run: |
          unzip -o -P "$ZIP_PASSWORD" app/signing/cablestat.keystore.zip -d app/signing/
          test -f app/signing/cablestat.keystore

      - name: Build Debug APK
        env:
          SIGNING_STORE_PASSWORD: ${{ secrets.SIGNING_STORE_PASSWORD }}
          SIGNING_KEY_PASSWORD: ${{ secrets.SIGNING_KEY_PASSWORD }}
        run: gradle -p "$GITHUB_WORKSPACE" clean assembleDebug --no-daemon --stacktrace

      - name: Verify APK signature
        run: |
          "$ANDROID_HOME/build-tools/34.0.0/apksigner" verify --print-certs app/build/outputs/apk/debug/app-debug.apk 2>&1 | tee /tmp/apk-certs.txt
          grep -qi "f67f0708d800bd1836de2757af60eab571984fb60d6974f5d3483b957f044d17" /tmp/apk-certs.txt

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: cablestat-debug-apk
          path: app/build/outputs/apk/debug/*.apk
          if-no-files-found: error